import React, { useState } from 'react'

export const useCountNumber = ({number}) => {
    const [count, setCount] = useState(0);


    const handleCount = (data)=> {
        
    }
  return (
    <div>useCountNumber</div>
  )
}
